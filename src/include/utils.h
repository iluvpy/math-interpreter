#ifndef UTILS_H
#define UTILS_H

#include <string.h>
#include <stdbool.h>
#include "defines.h"

// returns true if string is a digit 
// supports floating point numbers
bool string_digit(char *s);

#endif // UTILS_H