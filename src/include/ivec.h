// #pragma once
// commented out because its not used for now
// #include <stdlib.h>

// typedef struct ivec {
// 	int **vec;
// 	size_t length;
// } ivec;

// #define IVEC_SIZE sizeof(ivec)
// #define IVEC_PTR_SIZE sizeof(ivec*)

// ivec *generate_ivec();
// void ivec_append(ivec *vec, int n);
// int ived_get(ivec *vec, size_t index);
// void del_ivec(ivec *vec);